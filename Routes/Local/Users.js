/*
const usersRouter = require('express').Router();
const controller = require('../../Controllers/Local/Users');

// CRUD operations for users
usersRouter.get('/', controller.getAll); // Get all users
usersRouter.get('/:id', controller.getById); // Get a user by id
usersRouter.post('/', controller.create); // Create a new user
usersRouter.put('/:id', controller.update); // Update a user by id
usersRouter.delete('/:id', controller.delete); // Delete a user by id

module.exports = usersRouter;
*/